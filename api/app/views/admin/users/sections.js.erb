<%# document.getElementById("user_section_id").innerHTML(<%= escape_javascript(options_from_collection_for_select(@section)) %>
$("#user_section_id").html("<%= escape_javascript(options_from_collection_for_select(@sections, :id, :name)) %>");
